{
  "modid": "${ModID}",
  "name": "${project.name}",
  "author_list": [
    "ocean",
    "NuclearCyborg"
  ],
  "credits": "Carbon, freesound.org",
  "description": "${project.description}",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": [
    "basemod",
    "stslib"
  ]
}

